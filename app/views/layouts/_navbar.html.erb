<nav class="navigation">
  <div class="nav">
    <%= link_to 'APi-BattleShip', root_path, class: 'app-name' %>
    <%= link_to 'Dashboard', dashboard_path, class: 'nav-link' %>
    <%= image_tag("ship.png", alt: "ship", id: 'nav-image')%>
  </div>
  <div class="nav">
    <% if current_user %>
      <p class="nav-link">Logged in as <%= current_user.name %></p>
      <%= button_to 'Logout', logout_path, method: :delete, class: 'logout' %>
    <% else %>
      <%= render partial: 'sessions/new' %>
    <% end %>
  </div>
</nav>
